from django.urls import path
from chart import views

app_name = 'chart'

urlpatterns = [
    # path('chart/', views.ChartList.as_view(), name='chart-list'),
    # path('table/', views.TableList.as_view(), name='table-list'),
    # path('table/<int:pk>/', views.TableDetail.as_view(), name='table-detail'),
]